<div class="mb-6">
  <h1 class="text-2xl font-semibold text-gray-900">New Workflow</h1>
  <p class="mt-1 text-sm text-gray-500">Create a new workflow for processing inbox items</p>
</div>

<div class="max-w-lg rounded-lg border border-gray-200 bg-white p-6 shadow-sm">
  <%= render 'form', workflow: @workflow %>
</div>
